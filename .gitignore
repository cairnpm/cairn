# Nuxt dev/build outputs
.nuxt
.output
.nitro
.cache
dist

# Node dependencies
node_modules
*.log*
.npmrc

# Environment
.env
.env.*
!.env.example

# Misc
.DS_Store
.fleet
.idea
*.tsbuildinfo

# Local data (SQLite file)
.data

# CLI session cache (cached auth cookie — a credential)
.cairn

# Local working notes (not published)
docs/monetisation-distribution.md

# Private: real imported roadmap data (never publish)
roadmap.json
